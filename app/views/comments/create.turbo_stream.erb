<%# Append the new comment to the post's comments list and reset the form %>
<%= turbo_stream.append dom_id(@post, :comments) do %>
  <%= render @comment %>
<% end %>

<%= turbo_stream.replace dom_id(@post, :new_comment) do %>
  <%= render "comments/new", post: @post %>
<% end %>
