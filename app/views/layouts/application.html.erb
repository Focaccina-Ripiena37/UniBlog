<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Blog" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Blog">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

  <%# Includes the main stylesheet (application.css) which also brings in Action Text CSS %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag "https://cdn.simplecss.org/simple.css" %>
  </head>

  <body>
    <header>
      <nav style="display:flex; align-items:center; justify-content:space-between; gap:1rem; padding:0.5rem 0;">
        <div>
          <h1 style="margin:0;">Blog Progetto di Sistemi Web</h1>
          <p style="margin:0; color:#666;">Anno Accademico 2025/2026</p>
        </div>
        <div>
          <% if Current.user %>
            <span>👋 <%= Current.user.email_address %></span>
            <%= button_to "Logout", session_path, method: :delete, form: { style: "display:inline" } %>
          <% else %>
            <%= link_to "Login", new_session_path %>
            |
            <%= link_to "Registrati", new_user_path %>
          <% end %>
        </div>
      </nav>
    </header>
    <main>
      <%= yield %>
    </main>
    <footer>
      <p>2025 Blog. All rights reserved.</p>
    </footer>
  </body>
</html>
